#pragma once

// Total number of mixer channels (hardware channels for libdragon)
// This is the sum of channels allocated across all mix channels
#define AUDIO_MIXER_CHANNELS {audio_mixer_channels}

// Number of mix channels defined in Aura (e.g., "master", "music", "sfx")
#define AUDIO_MIX_CHANNEL_COUNT {audio_mix_channel_count}

// Mix channel indices (generated from Aura.mixChannels keys)
{mix_channel_defines}
